/* import 'package:flutter/material.dart';

class TitleBar extends StatelessWidget {
  final String title;
  final String titleColour;
  final int titleSize;
  const TitleBar({Key key, this.title, this.titleColour, this.titleSize})
      : super(key: key);

  final _tileStyle = TextStyle(
    fontSize: 
  );


  @override
  Widget build(BuildContext context) {
    return Row(
        mainAxisAlignment: MainAxisAlignment.spaceBetween,
        children: <Widget>[Text(this.title)]);
  }
}
 */